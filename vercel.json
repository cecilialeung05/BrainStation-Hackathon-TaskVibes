{
  "version": 2,
  "builds": [
    { "src": "server/api/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/quote/(.*)", "dest": "server/api/index.js" },
    { "src": "/(.*)", "dest": "server/api/index.js" }
  ]
}
